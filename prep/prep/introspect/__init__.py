"""introspect — schema introspection routes (SPEC §2.3, §7).

engine.py (Introspector protocol), sqlalchemy_introspector.py, duckdb_introspector.py
land in P3a. P0 ships this package marker so import paths resolve.
"""
