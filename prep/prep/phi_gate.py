"""phi_gate.py — the CI PHI gate (SPEC §2.5).

Asserts phi.json.phiColumnsetHash matches the runtime's, scans artifacts for raw
cells, and enforces sample_aggregate as the sole data path. P0 stub; lands in P3a.
"""
